
{#
<nav id="navigation" class="flex items-center justify-between flex-wrap bg-teal-500 p-6">
<div class="container">
    <div class="w-3/4 block flex-grow lg:flex lg:items-center">
  <div class="text-sm lg:flex-grow">
      <a class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" href="/">Home</a>
    </div>


  {% for entry in craft.entries.section('pages').eagerly().collect.all() %}
  <div class="w-3/4 block flex-grow lg:flex lg:items-center">
  <div class="text-sm lg:flex-grow">
      <a class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" href="{{entry.url}}">{{entry.title}}</a>
    </div>
  {% endfor %}
</div>
</nav> #}