
{%- import '_macros/atomic' as atoms -%}



<div class="container bg-amber-100 flex p-20">


      {# <div class="p-10 flex flex-col w-54 gap-8 text-lg"> #}
<div class="grid md:grid-cols-2 lg:grid-cols-2 gap-4 max-w-screen-xl">

{% for block in entry.contentBuilder.all() %}
  {% switch block.type %}

    {% case "cta" %}
      {{ block.render() }}

    {% case "images" %}
      {{ block.render() }}

    {% case "gallery" %}
      {{ block.render() }}

    {% case "featureList" %}
      {{ block.render({ loop, themeColor: "lightblue" }) }}

    {% case "relatedEntries" %}
     {{ block.render() }}

    {% case "table" %}
      {{ block.render() }}


    {% case "richText" %}
      {{ block.render() }}


    {% default %}


  {% endswitch %}

{% endfor %}


</div>
</div>